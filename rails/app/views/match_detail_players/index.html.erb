<p id="notice"><%= notice %></p>

<h1>Match Detail Players</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @match_detail_players.each do |match_detail_player| %>
      <tr>
        <td><%= link_to 'Show', match_detail_player %></td>
        <td><%= link_to 'Edit', edit_match_detail_player_path(match_detail_player) %></td>
        <td><%= link_to 'Destroy', match_detail_player, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Match Detail Player', new_match_detail_player_path %>
