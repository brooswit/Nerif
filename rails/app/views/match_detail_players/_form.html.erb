<%= form_for(match_detail_player) do |f| %>
  <% if match_detail_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match_detail_player.errors.count, "error") %> prohibited this match_detail_player from being saved:</h2>

      <ul>
      <% match_detail_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
